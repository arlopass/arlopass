---

---

<div
  class="flex items-stretch h-24 overflow-hidden border-t border-b border-[var(--ap-border)]"
  aria-hidden="true"
>
  {
    Array.from({ length: 120 }).map(() => (
      <div class="h-full flex-1 min-w-4 border-l border-[var(--ap-border)] bg-gradient-to-r from-[var(--ap-bg-base)] to-[var(--ap-bg-surface)]" />
    ))
  }
</div>
